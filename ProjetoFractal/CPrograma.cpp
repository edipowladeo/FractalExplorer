#include "Cprograma.h"


void Cprograma::apagar_todas_tarefas_livres_front()
{
//    for (auto it = TarefasFrontPresentes.begin(); it != TarefasFrontPresentes.end(); it++)
//    {
//        int status = (*it)->status;
//        if ((status!=2)&&(status!=1))
//        {
//            it = TarefasFrontPresentes.erase(it);
      // exemplo check past end it >>> if (ItrTarefa == Camada->TarefasFrontPresentes.end()) break;
//        }
//    }
}

  void  Cprograma::criarLink(CtarefaFront* TarefaFront, CtarefaBack* TarefaBack){
   // TarefaFront->TarefasBackPresentes.


    }

